<?xml version="1.0" encoding="UTF-8"?>
<!-- Test logging for cassyx-core.

     The embedded SSH server used by MinaSshTunnelTest logs every packet at DEBUG, which buries the
     surefire output that actually matters. The driver is equally chatty at connect time. Both are
     pinned to WARN here; cassyx's own logging stays at INFO so an unexpected warning is visible. -->
<configuration>
  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} -- %msg%n</pattern>
    </encoder>
  </appender>

  <logger name="org.apache.sshd" level="WARN"/>
  <logger name="com.datastax.oss.driver" level="WARN"/>
  <logger name="io.cassyx" level="INFO"/>

  <root level="INFO">
    <appender-ref ref="STDOUT"/>
  </root>
</configuration>
